spring:
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
      format-sql: true
      dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true